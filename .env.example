# docker compose 用の設定例。`.env` にコピーして使用してください。
# cp .env.example .env

# 公開ポート
APP_PORT=3000

# アクセス保護（Basic認証）
# インターネットに公開する場合は必ず設定してください。
# 未設定の場合は認証なしで全データが閲覧可能になります。
BASIC_AUTH_USER=sales
BASIC_AUTH_PASS=ここに十分に長いパスワードを設定してください
