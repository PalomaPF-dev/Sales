# ブラウザだけで公開する手順（ターミナル不要）

コマンド操作なしで、Webの管理画面だけで動く状態にするための手順です。

アプリのコードは既にGitHubへ反映済みで、Vercelへのデプロイはプッシュのたびに
自動で実行されています。残っているのは**データベースへの接続設定**だけです。

---

## 前提

- アプリ: Vercel
- データベース: **Neon（PostgreSQL）**

Vercelはリクエストのたびに使い捨ての環境でコードを動かすため、ファイルを保存しても
次のアクセスでは消えてしまいます。そのため、データはPostgreSQLに保存します。

社内ポータルと同じ **Neon** に相乗りできます。専用スキーマに分離するため、
ポータルのテーブルとは干渉しません。

---

## 手順1: 接続情報を用意する

Neonのダッシュボードから接続文字列を取得します。

```
postgresql://ユーザー名:パスワード@ep-xxxx.ap-southeast-1.aws.neon.tech/dbname?sslmode=require
```

> **プーラー経由ではない方を使ってください。**
> Neonは2種類の接続文字列を提供しており、ホスト名に `-pooler` が付く方は
> 接続プーラー（PgBouncer）を経由します。この経路では接続時のスキーマ指定が
> 無視されるため、本アプリでは **`-pooler` が付かない方** を指定してください。
> 誤って指定した場合は、起動時にその旨のメッセージが表示されます。

このアプリは **専用スキーマ `sales_pricing`** にテーブルを作成します。
ポータル等の既存テーブル（`users` や `sessions` など）と名前が衝突することはありません。
スキーマは初回アクセス時に自動作成されるため、事前のSQL実行は不要です。

---

## 手順2: Vercelに環境変数を設定する

1. <https://vercel.com> でプロジェクト `sales` を開きます
2. **Settings → Environment Variables** を開きます
3. 次を追加します（対象環境は Production / Preview 両方にチェック）

   | Name | Value |
   |---|---|
   | `DATABASE_URL` | 手順1の接続文字列 |

4. 必要に応じて追加します（任意）

   | Name | Value | 用途 |
   |---|---|---|
   | `DB_SCHEMA` | 例: `sales_pricing` | スキーマ名を変えたい場合 |
   | `DB_SSL_NO_VERIFY` | `true` | 社内認証局の証明書で接続が弾かれる場合 |
   | `BASIC_AUTH_USER` / `BASIC_AUTH_PASS` | 任意のID / パスワード | サイト全体に追加の認証をかけたい場合 |

5. **Deployments** タブを開き、最新のデプロイの「⋯」から **Redeploy** を実行します
   （環境変数は再デプロイで反映されます）

テーブルは初回アクセス時に自動で作成されるため、事前のSQL実行は不要です。

---

## 手順3: 動作を確認する

ブラウザで次のURLを開きます。

```
https://<デプロイ先>/api/health
```

`{"status":"ok","db":"postgres",...}` と表示されれば成功です。

`{"status":"not_configured",...}` の場合は、環境変数の値か対象環境（Production/Preview）を
見直してください。接続エラーの場合は、その内容がそのまま表示されます。

---

## 手順4: 最初のログインを作る

トップページを開くと **初期セットアップ画面** が表示されます。

1. 管理者（または営業企画部）を選ぶ
2. パスワードを設定する（10文字以上）
3. 「この内容で開始する」を押す

そのままログインした状態になります。この画面はパスワードが1つも設定されていない間だけ
表示され、設定後は二度と出てきません。

以降の担当者は、アプリの **ユーザー管理** 画面から追加できます。
追加時に仮パスワードが1度だけ表示されるので、本人にお伝えください。

---

## 手順5: 現行の管理表を取り込む

アプリの **Excel取込** 画面から、現行の管理表をアップロードします。
器具ごとに分かれたファイルは、順に取り込むと1つの一覧に統合されます。

> 1ファイル約4MBが上限です。これを超える管理表は、
> 分割していただくか、管理者にコマンド（`npm run import`）での投入を依頼してください。

### 取込時の注意

現行のExcelには、数値の列にメモ書きや `48,9％` のような表記が混ざっている箇所があります。
そのままでは数値として扱えないため、取込時に未設定（空欄）として記録し、件数を警告として表示します。

実データでは次の箇所が該当しました（金額計算には使われない列のため、集計値には影響しません）。

| 列 | 件数 | 例 |
|---|---|---|
| 1回目提示率 | 127件 | `48,9％` / メモ書き |
| 最終確定掛率 | 79件 | `48,9％` |

正しい値を反映したい場合は、元のExcelを修正してから取り込み直してください。

---

## 独自ドメインを使う場合

`sales.paloma-pf.com` を割り当てる手順は [DEPLOY.md](DEPLOY.md) に記載しています。

**注意**: Vercelの標準の保護（Vercel Authentication）は `*.vercel.app` にのみ適用され、
独自ドメインには適用されません。独自ドメインを設定すると社外からも到達可能になるため、
手順2の `BASIC_AUTH_*` を設定することをお勧めします。

アプリ自体は個人ごとのログインで保護されているため、未ログインでは価格データは表示されません。
