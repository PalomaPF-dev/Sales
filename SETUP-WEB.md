# ブラウザだけで公開する手順（ターミナル不要）

コマンド操作なしで、Webの管理画面だけで動く状態にするための手順です。
所要時間は10〜15分程度です。

アプリのコードは既にGitHubへ反映済みで、Vercelへのデプロイはプッシュのたびに
自動で実行されています。残っているのは**データベースの用意**と**環境変数の設定**だけです。

---

## なぜデータベースの設定が要るのか

Vercelはリクエストのたびに使い捨ての環境でコードを動かすため、
ファイルを保存しても次のアクセスでは消えてしまいます。
そのため、データを保持する場所を外部に用意する必要があります。

本アプリはSQLite互換の **Turso** を使う構成になっています
（無料枠で十分に収まります。本アプリのデータ量は23,000行・約3MB）。

---

## 手順1: データベースを作る

1. <https://turso.tech> を開き、GitHubアカウントなどでサインアップします
2. ダッシュボードで新しいデータベースを作成します
   - 名前: `sales-pricing`（任意）
   - リージョン: **東京（nrt）** を選んでください
     アプリも東京リージョンで動かしているため、離れた場所を選ぶと表示が遅くなります
3. 作成したデータベースの画面から、次の2つを控えます
   - **データベースURL**（`libsql://` で始まる文字列）
   - **アクセストークン**（画面上で作成・表示できます。長い文字列です）

> トークンは秘密情報です。チャットやメールに貼らず、次の手順で直接貼り付けてください。

---

## 手順2: Vercelに環境変数を設定する

1. <https://vercel.com> でプロジェクト `sales` を開きます
2. **Settings → Environment Variables** を開きます
3. 次の2つを追加します（対象環境は Production / Preview 両方にチェック）

   | Name | Value |
   |---|---|
   | `TURSO_DATABASE_URL` | 手順1で控えたURL |
   | `TURSO_AUTH_TOKEN` | 手順1で控えたトークン |

4. 社外からのアクセスを塞ぎたい場合は、次の2つも追加します（任意）

   | Name | Value |
   |---|---|
   | `BASIC_AUTH_USER` | 任意のID |
   | `BASIC_AUTH_PASS` | 十分に長いパスワード |

5. **Deployments** タブを開き、最新のデプロイの「⋯」から **Redeploy** を実行します
   （環境変数は再デプロイで反映されます）

---

## 手順3: 動作を確認する

ブラウザで次のURLを開きます。

```
https://<デプロイ先>/api/health
```

`{"status":"ok","db":"turso",...}` と表示されれば成功です。

`{"status":"not_configured",...}` の場合は、環境変数の値か対象環境（Production/Preview）を
見直してください。

---

## 手順4: 最初のログインを作る

トップページを開くと **初期セットアップ画面** が表示されます。

1. 管理者（または営業企画部）を選ぶ
2. パスワードを設定する（10文字以上）
3. 「この内容で開始する」を押す

そのままログインした状態になります。この画面はパスワードが1つも設定されていない間だけ
表示され、設定後は二度と出てきません。

以降の担当者は、アプリの **ユーザー管理** 画面から追加できます。
追加時に仮パスワードが1度だけ表示されるので、本人にお伝えください。

---

## 手順5: 現行の管理表を取り込む

アプリの **Excel取込** 画面から、現行の管理表をアップロードします。
器具ごとに分かれたファイルは、順に取り込むと1つの一覧に統合されます。

> 1ファイル約4MBが上限です。これを超える管理表は、
> 分割していただくか、管理者にコマンド（`npm run import`）での投入を依頼してください。

---

## 独自ドメインを使う場合

`sales.paloma-pf.com` を割り当てる手順は [DEPLOY.md](DEPLOY.md) に記載しています。

**注意**: Vercelの標準の保護（Vercel Authentication）は `*.vercel.app` にのみ適用され、
独自ドメインには適用されません。独自ドメインを設定すると社外からも到達可能になるため、
手順2の `BASIC_AUTH_*` を設定することをお勧めします。

アプリ自体は個人ごとのログインで保護されているため、未ログインでは価格データは表示されません。
